# 模块说明：数字丝路示例配置。
# 业务场景：开发者复制该文件后，为合规分析、视觉理解、数字人和分发服务补充自己的第三方服务参数。
# 核心职责：展示配置字段含义，不提供真实密钥。
app:
  name: "Huobao Drama API"
  version: "1.0.0"
  debug: true
  language: "zh"  # 系统语言：zh(中文) 或 en(英文)

server:
  port: 5678
  host: "0.0.0.0"
  cors_origins:
    - "http://localhost:3012"
  read_timeout: 600
  write_timeout: 600

database:
  type: "sqlite"
  path: "./data/drama_generator.db"
  max_idle: 10
  max_open: 100

storage:
  type: "local" # local 或 r2
  local_path: "./data/storage"
  base_url: "http://localhost:5678/static" # r2 时填写公开访问域名，例如 https://<your-public-domain>
  r2_account_id: ""
  r2_access_key_id: ""
  r2_secret_access_key: ""
  r2_bucket: ""
  r2_endpoint: "" # 可选，默认 https://<account_id>.r2.cloudflarestorage.com
  r2_region: "auto"

ai:
  default_text_provider: "openai"
  default_image_provider: "openai"
  default_video_provider: "doubao"

compliance:
  # DeepSeek/火山方舟走 OpenAI 兼容接口，合规服务只关心 base_url、endpoint、model 和 api_key。
  enabled: true
  base_url: "https://ark.cn-beijing.volces.com/api/v3"
  endpoint: "/chat/completions"
  api_key: ""
  model: "deepseek-v3-2-251201"

distribution:
  # Upload-Post 用于 Pinterest/Reddit 等社媒账号授权和发布任务，前端分发弹窗会读取这些默认展示配置。
  upload_post_base_url: "https://api.upload-post.com/api"
  upload_post_connect_title: "Connect Pinterest / Reddit"
  upload_post_connect_description: "Connect your own Pinterest and Reddit accounts before distributing content."
  upload_post_redirect_url: ""
  upload_post_logo_image: ""
  discord_username: "Drama Generator"
  discord_avatar_url: ""
  status_poll_interval_seconds: 20
  history_lookback_pages: 3

volcengine:
  # 视觉模型服务商品图片理解，语音配置服务数字人口播视频生成；密钥请在本地配置或环境变量中填写。
  access_key_id: ""
  secret_access_key: ""
  region: "cn-north-1"
  service: "cv"
  visual_host: "visual.volcengineapi.com"
  speech:
    app_id: ""
    token: ""
    cluster: "volcano_tts"
    endpoint: "https://openspeech.bytedance.com/api/v1/tts"
    submit_endpoint: "https://openspeech.bytedance.com/api/v3/tts/submit"
    query_endpoint: "https://openspeech.bytedance.com/api/v3/tts/query"
    resource_id: "volc.service_type.10029"
    namespace: "BidirectionalTTS"
    voice_type: ""
    clone_upload_endpoint: "https://openspeech.bytedance.com/api/v1/mega_tts/audio/upload"
    clone_resource_id: "volc.megatts.voiceclone"
    clone_project_name: "default"

# 分发网关（可选，用于接入真实社媒发布）
# 设置以下环境变量后，一键分发会向网关发起真实发布请求：
# DISTRIBUTION_GATEWAY_URL=https://your-distribution-gateway/api/publish
# DISTRIBUTION_GATEWAY_TOKEN=your-token
# DISTRIBUTION_GATEWAY_TIMEOUT_SECONDS=30
